{
    "Adaptive Tile-Based Shading for Mobile GPUs": {
        "Title": "Adaptive Tile-Based Variable Rate Shading for Energy-Efficient Mobile Rendering",
        "Problem Statement": "Mobile GPUs use tile-based rendering architectures but apply uniform shading rates across all tiles, wasting compute on low-complexity regions while undersampling high-detail areas, leading to suboptimal power-performance tradeoffs.",
        "Motivation": "Variable Rate Shading (VRS) on desktop GPUs allows per-tile shading rate control but mobile implementations are limited. Existing mobile approaches use simple screen-space heuristics that don't account for temporal coherence or perceptual importance. By analyzing per-tile content complexity at minimal cost and leveraging the tile-based architecture's on-chip memory, we can dynamically assign shading rates that concentrate compute on visually important regions while saving significant power on uniform areas.",
        "Proposed Method": "We propose a lightweight tile complexity analysis pass that runs before the main shading pass. For each tile, we compute a complexity score based on depth discontinuities, normal variation, and temporal motion vectors from the previous frame. Tiles with low complexity use coarse shading (1 shade per 2x2 or 4x4 pixels), while high-complexity tiles use full-rate shading. The complexity analysis uses a small compute shader operating on the G-buffer from the previous frame. We also introduce a temporal stability mechanism that smoothly transitions shading rates across frames to prevent flickering.",
        "Step-by-Step Experiment Plan": {
            "Step 1: Implement Tile Complexity Analysis": "Implement a compute shader that reads the previous frame's depth and normal buffers and computes per-tile complexity scores. Use a 16x16 tile size matching common mobile GPU tile sizes. The complexity score combines: (a) depth variance within the tile, (b) normal variance, (c) average motion vector magnitude. Normalize scores to [0,1] range.",
            "Step 2: Implement Shading Rate Assignment": "Map complexity scores to shading rates: score < 0.2 -> 4x4 coarse shading, 0.2 <= score < 0.5 -> 2x2 coarse shading, score >= 0.5 -> full-rate shading. Implement using Vulkan fragment shading rate extension (VK_KHR_fragment_shading_rate) or Metal variable rasterization rate.",
            "Step 3: Implement Temporal Stability": "Add a temporal filter that smooths shading rate transitions across frames. Use exponential moving average on complexity scores with alpha=0.3. Prevent shading rate from changing by more than one step per frame for any tile.",
            "Step 4: Prepare Test Scenes": "Use publicly available test scenes: Sponza Atrium (moderate complexity), Amazon Lumberyard Bistro (high complexity, mixed indoor/outdoor), and a simple outdoor terrain scene. Render all scenes at 1080p resolution.",
            "Step 5: Select Target Devices and Profiling Tools": "Test on: (a) Qualcomm Snapdragon 8 Gen 2 device with Adreno 740 GPU, using Snapdragon Profiler for GPU metrics, (b) MediaTek Dimensity 9200 device with Mali-G715 GPU, using ARM Streamline for bandwidth and power metrics, (c) Apple A17 Pro device, using Xcode Metal GPU debugger for performance analysis.",
            "Step 6: Establish Baselines": "Implement three baselines: (a) uniform full-rate shading (no VRS), (b) fixed 2x2 coarse shading everywhere (aggressive power saving), (c) simple luminance-gradient-based VRS from prior work without temporal stability.",
            "Step 7: Measure Performance and Quality": "For each scene and device combination, measure: average FPS and 1% low FPS, frame time breakdown (complexity analysis pass time vs shading pass time), GPU memory bandwidth via profiler, power consumption via battery monitoring over 10-minute continuous sessions. For visual quality: compute per-frame PSNR, SSIM, and LPIPS between the adaptive method and the full-rate reference rendering."
        },
        "Test Case Examples": {
            "Baseline (Uniform Full-Rate Shading)": "Scene: Sponza Atrium at 1080p, Device: Snapdragon 8 Gen 2. All tiles shaded at full rate. Performance: 45 FPS average, 2.8W GPU power draw. Quality: reference (PSNR = infinity). The GPU spends equal compute on flat walls and highly detailed vegetation.",
            "Proposed Method (Adaptive Tile-Based VRS)": "Scene: Sponza Atrium at 1080p, Device: Snapdragon 8 Gen 2. Tile complexity analysis runs in 0.15ms. Approximately 40% of tiles assigned to 4x4 coarse (flat walls, ceiling), 30% to 2x2 coarse (moderate detail floors, columns), 30% to full-rate (edges, vegetation, specular highlights). Performance: 62 FPS average (+38%), 1.9W GPU power (-32%). Quality: PSNR 38.5 dB, SSIM 0.987 (imperceptible quality loss).",
            "Explanation": "The adaptive method identifies that large portions of the Sponza scene (walls, ceiling) are spatially and temporally uniform and can be shaded at reduced rates without visible quality loss, while concentrating full-rate shading on edges and detailed geometry where it matters most for perceived quality."
        },
        "Fallback Plan": "If the adaptive VRS method does not achieve meaningful power savings (less than 10%) while maintaining quality (PSNR > 35 dB), we can investigate several directions. First, analyze which complexity features (depth, normal, motion) are most predictive of perceptual importance and refine the scoring function accordingly. Second, explore incorporating saliency-based perceptual metrics, where tiles near the screen center or containing salient objects receive higher shading rates. Third, the project can be reframed as an analysis paper studying the relationship between tile-level content complexity and optimal shading rates across different scene types (indoor vs outdoor, static vs dynamic) and mobile GPU architectures (Adreno vs Mali vs Apple), providing valuable guidance for future mobile VRS implementations."
    },
    "Temporal Super-Resolution for Mobile Games": {
        "Title": "MobileSR: Lightweight Temporal Super-Resolution for Tile-Based Mobile GPUs",
        "Problem Statement": "Mobile games often render at reduced resolution to meet frame rate targets, resulting in blurry or aliased output. Existing temporal upsampling methods like DLSS and FSR2 are designed for desktop GPUs and are too expensive for mobile.",
        "Motivation": "Desktop temporal upsampling achieves impressive quality but relies on heavyweight neural networks (DLSS) or complex temporal feedback loops (FSR2) that exceed mobile GPU budgets. By designing specifically for mobile tile-based architectures, leveraging jittered rendering with a lightweight recurrent filter, we can achieve high-quality upsampling with minimal compute overhead.",
        "Proposed Method": "MobileSR renders at lower internal resolution with per-frame sub-pixel jitter (Halton sequence). A lightweight compute shader accumulates samples into a history buffer at output resolution using motion vectors for reprojection. The accumulation uses confidence-weighted blending that detects disoccluded regions and fast motion. For areas with no valid history, an edge-directed spatial upsampler provides fallback. The pipeline adds 2 compute passes and ~1MB history buffer.",
        "Step-by-Step Experiment Plan": {
            "Step 1: Implement Jittered Rendering": "Add Halton sequence sub-pixel jitter to the projection matrix. Use a 16-sample sequence cycling every 16 frames. The jitter offset is applied in NDC space as a fractional pixel offset to the projection matrix.",
            "Step 2: Implement Motion Vector Generation": "Generate per-pixel motion vectors in the G-buffer pass. Combine camera motion (from current and previous view-projection matrices) with per-object motion for animated objects. Store as RG16F texture.",
            "Step 3: Implement History Reprojection": "In a compute shader, reproject each output pixel's position to the previous frame using motion vectors. Sample the history buffer at the reprojected UV coordinates using bilinear interpolation. Detect disocclusion by comparing reprojected depth against previous depth buffer.",
            "Step 4: Implement Confidence-Weighted Accumulation": "For each output pixel, compute blend confidence from: (a) color difference between reprojected history and current low-res sample (reject if > threshold), (b) motion vector reliability (lower confidence for large motion), (c) depth disocclusion test (reject if depth mismatch > 5%). Blend: output = confidence * 0.9 * history + (1 - confidence * 0.9) * current.",
            "Step 5: Implement Edge-Directed Spatial Fallback": "For pixels with confidence < 0.3 (no reliable history), use edge-directed upsampling: analyze 3x3 neighborhood gradients in the low-res image, determine dominant edge direction, interpolate along the edge rather than across it.",
            "Step 6: Test Configurations": "Test 2x upsampling (render at 540p, output 1080p) and 4x upsampling (render at 270p, output 1080p). Test on dynamic scenes with various motion characteristics: slow camera panning, fast action, rapid camera cuts.",
            "Step 7: Measure Metrics": "Compare against: bilinear upsampling, bicubic upsampling, AMD FSR 1.0 (spatial only), and simple TAA. Measure: PSNR and SSIM vs native 1080p reference, temporal stability (compute inter-frame difference metric to quantify flicker), frame time overhead of the upsampling passes, total power consumption over 5-minute sessions."
        },
        "Test Case Examples": {
            "Baseline (Bilinear 2x Upsampling)": "Scene: Fast-paced third-person action game, Device: Snapdragon 8 Gen 2. Render at 540p, bilinear upsample to 1080p. Performance: 60 FPS stable (14ms frame time saved by lower resolution). Quality: PSNR 28.3 dB vs native, SSIM 0.89. Visible blurring on character edges and environmental details, shimmer on thin features.",
            "Proposed (MobileSR 2x)": "Same scene and device. Render at 540p with Halton jitter, MobileSR temporal accumulation to 1080p. Reprojection pass: 0.8ms, accumulation pass: 0.6ms, total overhead: 1.4ms. Performance: 58 FPS (still well above 30 FPS target). Quality: PSNR 34.1 dB vs native, SSIM 0.96. Sharp character edges, stable fine details, clean thin features.",
            "Explanation": "MobileSR accumulates sub-pixel information across multiple jittered frames via temporal reprojection, effectively reconstructing high-frequency detail that purely spatial upsampling methods cannot recover, while the confidence-weighted blending prevents ghosting artifacts during motion."
        },
        "Fallback Plan": "If temporal accumulation produces unacceptable ghosting artifacts in fast-motion scenarios typical of mobile games, we can explore several alternatives. First, test more aggressive history rejection strategies that sacrifice temporal stability for reduced ghosting. Second, investigate a hybrid approach that uses temporal accumulation for slow-moving regions and spatial-only upsampling for fast-moving regions, based on per-pixel motion magnitude. Third, analyze which scene characteristics and motion patterns cause the most problems for temporal upsampling on mobile, and publish the findings as a guide for mobile game developers on when temporal vs spatial upsampling is appropriate."
    },
    "Lightweight Probe-Based Global Illumination for Mobile": {
        "Title": "Sparse Temporal Irradiance Probes for Real-Time Global Illumination on Mobile Devices",
        "Problem Statement": "Real-time global illumination (GI) on mobile devices is extremely challenging due to the high computational cost, yet the absence of GI makes mobile game scenes look flat and unrealistic.",
        "Motivation": "Baked lightmaps are static. Screen-space methods are limited in range. Probe-based methods like DDGI require desktop-class hardware. By using sparse probes with temporal amortization and simplified probe updates (tiny cubemap renders instead of ray tracing), we can provide convincing diffuse GI within mobile budgets.",
        "Proposed Method": "Place irradiance probes on a sparse world-space grid. Each probe stores a low-resolution (4x4) octahedral irradiance map and depth map for visibility. Instead of ray tracing, render tiny cubemap snapshots (16x16 per face) using LOD0 proxy geometry. Only 2-4 probes updated per frame via round-robin. At shading time, interpolate irradiance from 8 nearest probes with visibility-weighted trilinear blending.",
        "Step-by-Step Experiment Plan": {
            "Step 1: Implement Probe Grid": "Place probes on a regular 3D grid with configurable spacing (default: 2m). Store probe data in texture arrays: 4x4 octahedral irradiance (RGBA16F), 4x4 depth (R16F).",
            "Step 2: Implement Probe Updates": "For each probe update, render 6 faces of a tiny cubemap (16x16 each) using simplified scene geometry. Integrate cubemap into octahedral irradiance map. Use temporal round-robin: update 2-4 probes per frame.",
            "Step 3: Implement Probe Sampling": "In the fragment shader, find 8 nearest probes via grid lookup. For each probe, sample octahedral irradiance using surface normal direction. Weight by trilinear interpolation position and visibility (compare sample depth vs probe depth).",
            "Step 4: Test Scenes": "Indoor: furnished room with windows (dynamic sunlight). Outdoor: courtyard with overhangs (mixed sun/shadow). Use existing scene datasets.",
            "Step 5: Measure": "Compare: no GI, baked lightmaps (static reference), SSAO only. Metrics: PSNR/SSIM vs path-traced reference, per-frame GI cost (ms), power consumption, temporal stability of GI signal."
        },
        "Test Case Examples": {
            "Baseline (No GI, Direct Lighting Only)": "Indoor room scene on Snapdragon 8 Gen 2. Direct lighting only: 60 FPS, but scene looks flat with harsh shadows and no color bleeding between surfaces.",
            "Proposed (Sparse Temporal Probes)": "Same scene. Probe grid: 5x3x5 = 75 probes, 2m spacing. Update 3 probes/frame. GI adds soft indirect light, color bleeding from red wall onto white floor. Cost: 0.5ms per frame. Quality: PSNR 31.2 dB vs path-traced reference (vs 24.8 dB for no-GI baseline).",
            "Explanation": "The sparse probe system captures room-scale light transport including color bleeding at negligible cost by amortizing probe updates across frames and using simplified geometry for probe rendering."
        },
        "Fallback Plan": "If probe-based GI shows too much temporal lag when lighting changes rapidly, investigate: (1) prioritized probe updates based on lighting change detection, (2) hybrid approach combining probes for low-frequency GI with screen-space methods for high-frequency details, (3) analyze the minimum probe density and update rate needed for different scene types."
    },
    "Bandwidth-Optimal Shadow Mapping for Mobile": {
        "Title": "Tiled Shadow Mapping: Bandwidth-Efficient Shadows for Mobile Tile-Based GPUs",
        "Problem Statement": "Shadow mapping is one of the most bandwidth-intensive operations in mobile rendering, consuming precious memory bandwidth on tile-based mobile GPUs.",
        "Motivation": "Mobile tile-based GPUs have fast on-chip tile memory but expensive off-chip bandwidth. By redesigning shadow computation to work within the tile memory hierarchy, computing per-tile shadow masks during the shadow pass and consuming them during shading without additional off-chip fetches, we can dramatically reduce shadow bandwidth.",
        "Proposed Method": "Tiled Shadow Mapping (TSM) computes shadows per-tile. For each screen tile, identify needed shadow cascades and fetch only relevant texels. Store a compact per-tile shadow mask in tile memory for use during shading. Use a single shadow atlas with tight viewport packing. Add temporal shadow caching for static regions.",
        "Step-by-Step Experiment Plan": {
            "Step 1: Implement Shadow Atlas": "Pack all shadow maps (directional CSM cascades + point/spot lights) into a single texture atlas with tight viewport packing to minimize wasted space.",
            "Step 2: Implement Per-Tile Shadow Analysis": "In a compute pass, for each screen tile determine which shadow map regions are needed based on tile depth range and light frustums. Generate a per-tile fetch list.",
            "Step 3: Implement Tile-Local Shadow Evaluation": "During the main shading pass, evaluate shadows using the pre-fetched shadow data stored in tile memory. Output a 1-bit-per-sample shadow mask per tile.",
            "Step 4: Implement Temporal Caching": "Track which shadow map regions contain only static casters. Cache these regions and only re-render when dynamic objects enter the shadow frustum.",
            "Step 5: Test Scenes and Metrics": "Outdoor game scenes with 1 directional + 4 point lights. Compare: standard 4-cascade CSM, VSM, screen-space shadows. Measure: shadow quality vs reference, frame time, memory bandwidth (GPU profiler), power consumption on 3 mobile devices."
        },
        "Test Case Examples": {
            "Baseline (Standard 4-Cascade CSM)": "Outdoor village scene, Snapdragon 8 Gen 2. 4 cascade shadow maps at 1024x1024 each. Shadow pass: 2.1ms, bandwidth: 45MB/frame for shadow operations. Total shadows cost: 3.8ms.",
            "Proposed (Tiled Shadow Mapping)": "Same scene. Single 2048x2048 atlas. Per-tile shadow analysis: 0.3ms. Shadow evaluation within tile memory: 1.2ms. Temporal caching eliminates 60% of shadow re-renders for static objects. Total: 1.5ms, bandwidth: 18MB/frame (-60%).",
            "Explanation": "TSM reduces shadow bandwidth by computing shadows within tile memory boundaries and caching static shadow regions, avoiding redundant off-chip memory traffic."
        },
        "Fallback Plan": "If tile-local shadow evaluation doesn't provide sufficient quality for soft shadows, explore: (1) per-tile PCF with optimized filter patterns, (2) combine with contact-hardening shadow approximation, (3) analyze the bandwidth-quality tradeoff across different shadow techniques on mobile as a reference study."
    }
}
