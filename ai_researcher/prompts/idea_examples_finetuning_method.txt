{
	"Neural Texture Compression for Mobile": {
		"Type": "neural_graphics",
		"Problem": "Mobile devices have limited GPU memory and bandwidth, making high-quality texture storage and streaming a major bottleneck for visually rich 3D applications.",
		"Existing Methods": "Standard texture compression formats (ASTC, ETC2) provide fixed compression ratios. Neural texture compression methods like NTC achieve better ratios but require expensive decompression infeasible on mobile GPUs.",
		"Motivation": "We can design a neural texture representation specifically optimized for mobile GPU architectures. By using tiny neural networks that fit within shader core capabilities and leveraging tile-based memory hierarchy, we can achieve higher compression ratios than ASTC while maintaining real-time decompression.",
		"Proposed Method": "We propose MobileNTC, where each texture is encoded as a compact latent grid plus a tiny decoder network (< 50K parameters). The decoder is a series of 1x1 convolutions mapping to efficient shader operations on mobile GPUs. During rendering, texture lookups sample from the latent grid and run the decoder in the fragment shader. We train per-texture using L1 + LPIPS loss and introduce hardware-aware quantization to FP16/INT8 formats natively supported by mobile GPU ALUs.",
		"Experiment Plan": "Evaluate on standard texture datasets and in-game texture atlases. Compare against ASTC at various quality levels and desktop NTC. Measure compression ratio, decompression speed, visual quality (PSNR, SSIM, LPIPS), and GPU memory savings on Adreno and Mali GPUs."
	},
	"Mobile Neural Radiance Caching": {
		"Type": "neural_graphics",
		"Problem": "Computing indirect illumination in real-time is infeasible on mobile GPUs, leading to flat-looking scenes in mobile games without costly precomputed solutions.",
		"Existing Methods": "Neural Radiance Caching (NRC) on desktop uses neural networks to cache radiance values, but the network evaluation cost is too high for mobile. Spherical harmonics probes are lightweight but low-fidelity.",
		"Motivation": "By distilling radiance information into an extremely compact neural representation that can be evaluated in a single shader pass, we can bring neural radiance caching to mobile. The key is to trade spatial resolution for network simplicity, using a hash-grid encoding that enables constant-time lookups.",
		"Proposed Method": "We train a tiny MLP (3 layers, 32 neurons each) with a multi-resolution hash grid encoding to predict irradiance at any world-space point and normal direction. The hash grid is stored as a compact texture atlas. At shading time, the fragment shader looks up hash grid features via texture fetches and evaluates the MLP. The network is trained offline on path-traced reference data for each scene. For dynamic lighting, we fine-tune only the last MLP layer using a few training samples per frame generated from simplified probe renders.",
		"Experiment Plan": "Test on indoor game scenes with complex lighting. Compare against baked lightmaps, SH probes, and screen-space GI. Measure visual quality (PSNR, SSIM vs path-traced reference), inference time per pixel, total frame time overhead, and power consumption on mobile devices."
	},
	"Distilled Neural LOD for Mobile": {
		"Type": "neural_graphics",
		"Problem": "Traditional LOD (Level of Detail) systems use hand-authored or algorithmically simplified mesh versions, which require significant storage and often show visible popping artifacts during transitions.",
		"Existing Methods": "Progressive meshes allow continuous LOD but require CPU-side mesh manipulation. Nanite-style virtualized geometry uses GPU-driven mesh simplification but requires desktop-class hardware. Neural implicit representations like NeRF offer continuous LOD but are too slow for mobile real-time rendering.",
		"Motivation": "We can train a compact neural network to represent a mesh at continuous LOD levels, then deploy it on mobile using efficient inference. By representing geometry as a neural signed distance field (SDF) that can be sphere-traced at variable resolution, we achieve smooth LOD transitions without storing multiple mesh versions.",
		"Proposed Method": "For each asset, we train a small neural SDF (2 layers, 64 neurons, with hash-grid positional encoding) that represents the object's geometry. At render time, we sphere-trace the neural SDF in the fragment shader, with the number of march steps adapted based on the object's screen-space size (fewer steps = coarser LOD). To make this practical on mobile, we: (1) quantize the network to INT8, (2) use a bounding volume to skip empty space, (3) cache sphere-tracing results temporally. For objects covering many pixels, we fall back to traditional mesh rendering via a hybrid system.",
		"Experiment Plan": "Test on game-quality assets (characters, props, buildings) across different screen sizes. Compare against traditional discrete LOD, progressive meshes, and Nanite-style approaches. Measure visual quality at different LOD levels, rendering speed, memory usage, and power consumption on mobile devices."
	},
	"Lightweight Neural Post-Processing for Mobile": {
		"Type": "neural_graphics",
		"Problem": "Post-processing effects like denoising, super-resolution, and tone mapping significantly improve visual quality but existing neural approaches are too computationally expensive for mobile real-time rendering.",
		"Existing Methods": "Desktop neural post-processing uses large U-Net architectures or transformer-based models. Mobile engines use simple hand-tuned filters (bilateral, Gaussian) that produce lower quality results. Some mobile-optimized neural networks exist but still consume 3-5ms per frame.",
		"Motivation": "We can design a unified neural post-processing network that performs multiple effects (denoising, super-resolution, color grading) in a single pass, amortizing the network overhead across multiple effects. By using depthwise-separable convolutions and a shared feature backbone, the marginal cost of adding each effect becomes very small.",
		"Proposed Method": "We propose MobilePostFX, a unified neural post-processing pipeline. The architecture consists of: (1) a shared lightweight encoder (3 depthwise-separable conv layers) that extracts features from the input frame, (2) parallel task heads for each post-processing effect (denoising, upsampling, tone mapping), each consisting of a single 1x1 conv layer. The entire network has ~20K parameters and processes a 1080p frame in under 1ms on mobile GPUs. We train end-to-end using a multi-task loss combining reconstruction loss for each effect. The network inputs include the rendered frame, motion vectors, and depth buffer, enabling temporally stable results.",
		"Experiment Plan": "Test on rendered game frames at various quality levels. Compare against: (1) no post-processing, (2) individual hand-tuned filters, (3) individual neural networks per effect. Measure combined visual quality improvement (PSNR, SSIM, LPIPS), total processing time, and power consumption. Test on Snapdragon, MediaTek, and Apple mobile SoCs."
	}
}
